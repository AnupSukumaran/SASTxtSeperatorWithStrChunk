import XCTest
@testable import SASTxtSeperatorWithStrChunk

final class SASTxtSeperatorWithStrChunkTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SASTxtSeperatorWithStrChunk().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
